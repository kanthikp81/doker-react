sudo: required
services:
  - docker

before_install:
  - docker build -t kanthikp81/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kanthikp81/docker-react npm tun test -- --coverage